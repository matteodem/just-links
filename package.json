{
  "scripts": {
    "dev": "poi dev index.js",
    "build": "poi build index.js",
    "deploy": "yarn build && surge ./dist --domain just-links.surge.sh"
  },
  "dependencies": {
    "vuex": "^2.3.1",
    "vuex-localstorage": "^1.0.0"
  }
}
